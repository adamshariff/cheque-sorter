# Python Cheque Generator

Run this script with Python 3 after installing Pillow:

```bash
pip install pillow
python generate_cheques.py
```

It will generate PNG cheque images in the exports folder.
